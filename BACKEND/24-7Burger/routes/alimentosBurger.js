var express = require('express');
var router = express.Router();
var mongoose = require('mongoose');
var db = mongoose.connection;
const { } = require('./../controllers/alimentosController');

module.exports = router;